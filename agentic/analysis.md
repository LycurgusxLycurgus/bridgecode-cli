# Bridgecode 4.3 — completed implementation

## Completed private-document reassessment

User requested one rapid comparison with the newly supplied Primitives-Correction protocol. Route LEAN/PATCH: research used the supplied framework plus private note and relevant implementation; Q&A was unnecessary because the output contract was explicit; the plan required a warranted verdict and all eight outputs; execution changed only the private note and this record; review checked conceptual dependency/relocation and privacy; condensation records the outcome here. Status: COMPLETE; REPLAN count 0.

Acceptance passed: formal verdict V2 retained, document revision 2.1 distinguished from protocol verdicts, four primitives retained, all eight outputs present, fundamental concepts/explanations explicitly constructed from Π/R/C, abstract System/Representation alternative reconstructed and limited by relocation analysis. Fresh read-only reviewer returned PASS. Git check-ignore confirms exclusion, and the unchanged npm allowlist excludes agentic/; prior artifact evidence remains identified as prior. No runtime files, package version or architecture changed; no runtime test rerun was needed for this prose-only revision. Memory check: existing architecture description remains accurate.

Status: COMPLETE for the local implementation scope, 10 September 2026. Independent review: PASS. REPLAN count: 0. No publication, commit, real-project installation, or live host activation was performed. Next action requires the user's release/adoption decision.

## Decision and scope

The user approved a compact permanent core with six lazy-loaded specialists, Best Agent2 and shared Taste2, decision-based native perspective/amalgam questions, direct frontend authorship with three initial reference images, one coordinated real acceptance block, and a fresh native reviewer. Causal defects are corrected in code; architectural change is appropriate when the cause is structural. Verified implementation and regression protection are mapped in architecture before individual legacy rules are removed.

Route: ROBUST because instruction policy, ownership, migration schema, and release behavior interact. Research was full because supplied references and actual harness/lifecycle behavior controlled the design. Q&A resolved user-owned policy choices before execution. The plan used explicit observable boundaries to prevent underspecification and unrelated improvements. Execution changed source, package, lifecycle, and supporting tests coherently. Review used real disposable-project regressions and fresh independent agents because updates can affect repository data. Condensation maps enforced safeguards to code/tests and preserves a private explanatory audit after validation.

Baseline: canonical 4.1.0 at bfd1c5c411f3f5f85a005e82558567280ef785c5. This package and sibling codex_condensation were identical before the change; the 4.3 payload copies are also byte-identical.

## Acceptance and observed proof

- [x] Core/specialists: permanent root policy and six explicit triggers; source parity and static anchors pass. Full supplied Best Agent/Taste/design mechanisms retained; writing/copywriting use shared Taste.
- [x] Lifecycle: production install, true no-op, update, bootstrap preservation, projected dry-run and actual doctor verified by tests.
- [x] Migration: marked/unmarked canonical 4.1, mixed-EOL Unicode rules, future supported release fixture, and arbitrary architecture/analysis memory preservation pass.
- [x] Ownership/containment: linked parents, unsafe/case-aliased and protected paths, unowned new files even when identical, canonical tampering, malformed markers, and edited obsolete files are rejected without target writes.
- [x] Transactions: precondition conflicts, injected interruption, failed post-check, complete rollback, retained outside edits and journal, and guarded recovery exercised.
- [x] Hooks: installed script tested with heartbeat/compact/unrelated events and integrity fallback; unrelated hook configuration survives and disabling is verified.
- [x] Distribution: exact packed executable installs/updates/passes doctor; allowlist, version expression, tag gate and tested-artifact publication wiring checked. No publish command executed.
- [x] Policy walkthroughs: routes, perspective cards, causal condensation, design scopes, recovery and reviewer transitions traced below; these are simulations, not performance certification.
- [x] Independent review: fresh read-only agents returned FIX, FIX, then PASS after concrete repairs and complete reruns. Last reviewer independently ran 36/36 tests. Native inheritance was used; actual matching model/effort identity is not independently exposed.
- [x] Condensation: architecture.md maps responsible code/tests; primitives-private.md created after PASS, gitignored and excluded from npm; fixtures cleaned and existing user release archive preserved.

Final declared block: `npm test` and `npm run test:release`. Observed result: 36/36 local tests plus 1/1 exact-artifact regression passed, zero skips, on Windows/Node 24. The source synchronization check is intentionally unavailable only in standalone package checkouts without the sibling authoring source. Git diff whitespace check passed.

The release regression packs once, installs its actual executable into a disposable host, exercises a disposable target, verifies the package allowlist, and removes fixtures/cache. The publish workflow resolves that tested artifact's version through a separately tested non-publishing Node expression.

Review corrections are now enforced in production: update collisions require prior ownership, Git/agent configuration paths are reserved, parsers reject additional malformed marker tokens, and the release expression uses valid shell/JavaScript quoting. Mixed-EOL legacy reconstruction was also corrected during root testing. See architecture.md for exact owners and test files.

## Manual policy traces and evidence limits

A known patch uses LEAN/PATCH, a compact checklist, no invented questions, real changed-boundary acceptance, native review, and an explicit condensation check. A bounded causal defect uses DEBUG and corrects the responsible code; uncertainty crossing boundaries escalates the route. A consequential migration with settled requirements uses ROBUST research without an unnecessary pause.

An unresolved design choice reaches one or two native cards after research. Options explain Person/Core/representative work or distinct complementary responsibilities, concrete consequences and recommendation fit. Dependent work waits; independent authorized work continues. Full new frontend work loads Design/Taste, creates three reference images, implements directly, validates real behavior, and finalizes verified DESIGN.md. Existing-direction maintenance reuses the guide and source without automatic reference regeneration. Unavailable image generation leaves the full design workflow explicitly provisional.

An exact read-only response preserves its form and makes no repository writes. Compaction or model change recovers core, active contract/checklist and REPLAN count, relevant source through architecture, and required specialists. FIX repairs return through the complete declared block and fresh review. The second REPLAN stops the automatic loop and preserves evidence; timeouts never count as verdicts.

Already-implemented legacy rules are verified against code/tests/constraints, mapped into architecture, then individually removed. Unresolved causal corrections are implemented only within authorized scope, verified and documented before removal. Rules outside scope remain visible and urgent.

These are manual instruction traces; hook subprocess fixtures establish protocol, not live trusted-host delivery. Static anchors establish selected wording, not model compliance. No coding-performance benchmark, Linux CI run, registry installation of 4.3, or independent model/effort identity certification is claimed.

The permanent core is 13,326 UTF-8 bytes; the prior read-all operating layer was 55,812 bytes. All new instructions plus human guide total 99,285 bytes because full specialists retain depth. These are unique source-byte measurements, not token/cost measurements; full specialist loading can be larger for demanding tasks.

## Handoff

Start from `agentic/architecture.md` for the implementation map, this file for acceptance/evidence, `README.md` for installation/release procedure, and `AGENTS.md` plus triggered specialists for policy. The authoring source is `../codex_condensation`; run `npm run sync:payload` after source edits. Any code repair must rerun the declared block and receive fresh review.

A future authorized release should retain the tested artifact with `npm run test:release -- --output release`, confirm tag/version and npm publishing setup, publish that exact artifact, and then test the registry version in a disposable repository. Live hook activation still requires a trusted-host check. The private primitives note is for the user's separate backup, never runtime context.
